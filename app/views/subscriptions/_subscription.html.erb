<div class="column">
  <div class="card">
    <img src="<%= subscription.teacher.avatar %>" width="100%">
    <h3><%= teacher_full_name(subscription.teacher) %></h3>
    <p><%= subscription.teacher.email %></p>
  </div>
</div>
