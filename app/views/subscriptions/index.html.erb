<section class="container">
  <div class="right-container">
    <% if @course.nil? %>
      <div class="row">
        <div class="column"><h2>All Subscriptions</h2></div>
      </div>
    <% else %>
      <div class="row">
        <%= render partial: './courses/course', locals: { course: @course } %>
      </div>
    <% end %>
    <div class="row">
      <% @subscriptions.each do |subscription| %>
        <%= render partial: 'subscription', locals: { subscription: subscription } %>
      <% end %>
    </div>
    <div class="pagination-row">
      <%== pagy_nav(@pagy) %>
    </div>
  </div>
</secion>
